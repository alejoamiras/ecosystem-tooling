# ── deps / builds ────────────────────────────────────────────────────────────
node_modules/
dist/
target/
export/
src/artifacts/
action/dist/
codegenCache.json
*.tsbuildinfo
*.tgz
packages/aztec-standards/artifacts/
packages/aztec-standards/deployments.json

# ── aztec / test state ───────────────────────────────────────────────────────
log/
*.log
store*/
pxe_data*/
wallet_data*/
pxe-test*/
benchmarks/*.benchmark.json
.run-state/

# ── local-only (never published with the repo) ──────────────────────────────
.claude/
implementations-plan/

# ── env / OS ─────────────────────────────────────────────────────────────────
.env
.env.local
.DS_Store
