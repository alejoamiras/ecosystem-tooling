# ── deps / builds ────────────────────────────────────────────────────────────
node_modules/
dist/
target/
export/
src/artifacts/
action/dist/
codegenCache.json
*.tsbuildinfo
*.tgz

# ── aztec / test state ───────────────────────────────────────────────────────
log/
*.log
store*/
pxe_data*/
wallet_data*/
pxe-test*/
benchmarks/*.benchmark.json
.run-state/

# ── local-only (never published with the repo) ──────────────────────────────
.claude/
# no trailing slash: also matches a worktree-local SYMLINK to the root clone's dir
implementations-plan

# ── env / OS ─────────────────────────────────────────────────────────────────
.env
.env.local
.DS_Store
