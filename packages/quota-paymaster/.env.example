# Local development / tests (defaults shown; all optional)
# NODE_URL=http://localhost:8080
# L1_RPC_URL=http://127.0.0.1:8545

# Phase-1 lineage verification only: a READ-ONLY Aztec mainnet node RPC used once to
# confirm the compiled class id matches the live deployment. No key material, no writes.
# MAINNET_NODE_URL=
